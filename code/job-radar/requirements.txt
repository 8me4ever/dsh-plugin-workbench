# job-radar 依赖
# V0 核心(规则打分,无需 LLM)
PyYAML>=6.0
feedparser>=6.0
httpx>=0.27

# 仪表盘
fastapi>=0.115
uvicorn>=0.30

# 测试
pytest>=8.0

# BOSS直聘采集器(scripts/boss.py,浏览器自动化)
playwright>=1.45

# 简历 PDF 解析(读 resume/*.pdf 用)
pdfplumber>=0.11
